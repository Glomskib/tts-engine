node_modules
.env
.env.local
.env.*.local
.next
dist
supabase/.temp

# Claude local config
.claude/

# Windows reserved filenames (can't be deleted)
nul

.env*.local

# Cron / runtime logs (auto-generated, should not be tracked)
web/data/ri-logs/
web/data/ri-logs/*

# Browser automation session state — never track
browser-service/auth-state.json
browser-service/auth-state*.json
browser-service/cookies/
*.bak
